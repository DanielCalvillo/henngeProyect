import { makeStyles } from '@material-ui/core/styles';

const ResultNumberStyles = makeStyles(() => ({
    results: {
        fontFamily: 'tondo, sans-serif',
        fontStyle: "normal",
        fontWeight: "900",
        color: "#737373",
        paddingLeft: "1rem"
    }
}));

export default ResultNumberStyles